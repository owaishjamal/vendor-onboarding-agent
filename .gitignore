__pycache__/
*.py[cod]
.venv/
venv/
.env
node_modules/
dist/
data/cases.db
data/cases.db-*
data/.llm_cache/
.pytest_cache/
.DS_Store

# Runtime document uploads. Vendor submissions land here at run time; they
# are per-run artefacts, not source, and the case record in the database is
# what decisions are audited against.
data/documents/uploads/

# TypeScript / Vite build artefacts
*.tsbuildinfo
vite.config.ts.timestamp-*.mjs

# Agent tooling. .claude/ IS committed — the instructions, commands, sub-agents
# and hooks are shared, and a guard that only exists on one machine guards
# nothing. Personal overrides go in settings.local.json.
.claude/settings.local.json
.mcp.local.json
